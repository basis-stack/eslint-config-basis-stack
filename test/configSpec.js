import { expect } from 'chai';

import { the, should } from './utils';
import config from './../';

the('config', () => {
    
  should('do things', () => {

    expect(true).to.equal(true);
  });
});